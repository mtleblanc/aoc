{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug AOC",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceFolder}/build-debug/aoc",
            "args": [],
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "CMake: Build All (Debug)"
        }
    ]
}
